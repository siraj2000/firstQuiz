import 'package:flutter/material.dart';

class ColorTheme {
  // Light colors
  static const Color primary = Color(0xFF6A1B9A);
  static const Color bgLight = Colors.white;
  static const Color textLight = Colors.black;
  static const Color textBlue = Color.fromARGB(255, 24, 79, 217);

  // Dark colors
  static const Color bgDark = Color(0xFF0E0E0E);
  static const Color cardDark = Color(0xFF1A1A1A);
  static const Color textDark = Colors.white;
  static const Color accentDark = Color.fromARGB(255, 217, 182, 223);
}
